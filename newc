### 두 수를 메모리에 저장하고 더하기
```
#include <iostream>
int Add(int a, int b) {
    return a + b;
}
int main() {
	int a(2), b(3);
    std::cout << Add(a, b);
    return 0;
}
```
### 두 수를 함수를 이용하여 더하기
```
#include <iostream>
int Add(int a, int b) {
    return a + b;
}
int main() {
	int a(2), b(3);
    std::cout << Add(a, b);
    return 0;
}
```
### 동적 메모리 할당 및 활
```
#include <iostream>
int main() {
    int* ptr = new int;
    *ptr = 10;
    cout << *ptr;
    delete ptr;
    return 0;
}
```
### 범위기반 for 문
```
#include <iostream>
#include <vector>

int main() {
    std::vector<int> v = {1, 2, 3, 4, 5};

    for (int i : v) {
        std::cout << i << " ";
    }

    return 0;
}
```
